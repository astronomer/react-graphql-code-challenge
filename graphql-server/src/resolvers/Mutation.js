const Mutation = {
  createUser(parent, args, { db }, info) {
    /*
    * Complete this function to create a new user.
    * Email should be unique to the db.
    * Consider security when storing passwords.
    * Feel free to add packages if nessessary.
    */
  },
};

export default Mutation;
